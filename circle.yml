database:
  post:
    - psql -c "CREATE EXTENSION pg_tgrm;" -d circle_test
dependencies:
  cache_directories:
    - "buttonwood"
    - "web"
machine:
  ruby:
    version: ruby-2.2.3
  node:
    version: 4.1.1
