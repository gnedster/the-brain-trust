extends ../layout

block content
  h2 whoa

  p.
    Looks like you didn't authorize
    #[a(href='/applications/#{application.name}') #{application.name}].
    Click #[a(href='#{application.getAuthorizePath("slack", oAuthState)}') here]
    to try that again.
